# Core dependencies (required)
pyyaml>=6.0
python-dotenv>=1.0.0
feedparser>=6.0.0
requests>=2.28.0

# PDF text extraction (optional but recommended)
PyPDF2>=3.0.0

# HTML parsing (optional)
beautifulsoup4>=4.12.0

# Email delivery - choose one or none
# resend>=0.7.0          # For Resend email delivery
# sendgrid>=6.10.0       # For SendGrid email delivery

# Markdown to HTML conversion (for email)
markdown>=3.5.0

# Google Scholar - optional, often gets rate limited
# scholarly>=1.7.0

# Note: The following are NOT pip packages:
# - rclone: Install from https://rclone.org/install/
# - Claude CLI: Install with npm install -g @anthropic-ai/claude-code
